# .github/workflows/feature-update-pr.yml
name: "PR: develop → active feature/*"

on:
  workflow_dispatch:
    inputs:
      dry_run:     # true にすると PR を作らずログだけ
        description: "Preview only (no PR creation)"
        type: boolean
        default: false

jobs:
  create-prs:
    runs-on: ubuntu-latest

    permissions:
      pull-requests: write
      contents: read

    steps:
      - uses: actions/checkout@v4
      - name: Scan and open PRs
        run: |
          set -euo pipefail

          repo="${{ github.repository }}"
          cutoff=$(date -u -d '-7 days' +%s)
          dry="${{ github.event.inputs.dry_run || 'false' }}"

          echo "==> Collecting feature/* branches updated since $(date -u -d '-7 days')"
          branches=$(gh api "/repos/${repo}/branches?per_page=100" \
                       --paginate --jq '.[].name' | grep '^feature/')

          for br in $branches; do
            # 取得したブランチの最新 push 日を確認
            pushed=$(gh api "/repos/${repo}/branches/${br}" --jq '.commit.commit.committer.date')
            pushed_ts=$(date -d "$pushed" +%s)

            if [[ $pushed_ts -lt $cutoff ]]; then
              echo "⏭  $br skipped (last push $pushed)"
              continue
            fi

            echo "🔍  $br is active (last push $pushed)"

            # 既存の “develop → $br” PR が開いているかチェック
            existing=$(gh pr list --base "$br" --head develop --state open \
                                  --json number -q '.[0].number')

            if [[ -n "$existing" ]]; then
              echo "↳  PR #$existing already exists; skipping"
              continue
            fi

            if [[ $dry == "true" ]]; then
              echo "💡  (dry-run) Would create PR develop → $br"
              continue
            fi

            echo "✏️  Creating PR develop → $br ..."
            gh pr create \
              --base "$br" \
              --head develop \
              --title "Sync develop into $br" \
              --body "Automated PR to keep **$br** up-to-date with **develop** (last push: $pushed)."
          done

          echo "✅  Finished"
